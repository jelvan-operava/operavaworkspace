# Server-side Gemini AI Assistant key (required).
# Get a key from Google AI Studio: https://aistudio.google.com/app/apikey
# Do NOT prefix with NEXT_PUBLIC_ — this must stay server-only.
GEMINI_API_KEY=
